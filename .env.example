# Copia este archivo como ".env" (sin .example) y pega ahí la cadena real.
# El archivo ".env" NUNCA se sube a git (ya está en .gitignore).

DATABASE_URL=postgresql://usuario:password@host:5432/nombre_db
